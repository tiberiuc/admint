<h1 class="title"><%= Admint.Page.page_title(@admint.page_opts) %></h1>
<table class="table is-hoverable">
  <thead>
    <tr>
      <th><%= checkbox(:index, :select_all, phx_click: "toggle_select_all",  phx_target: @myself, value: @admint.index.select_all) %></th>
      <%= for {_id,field} <- @admint.fields do %>
        <th><%=field.label %></th>          
      <% end %>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= for {id, row} <- @admint.data do %>
      <tr id=<%=id %>>
          <td><input type="checkbox"/></td>
          <%= for {id,_field} <- @admint.fields do %>
           <td><%=Map.get(row, id) %></td>          
          <% end %>
          <td>
            <button class="button is-small is-light mb-0" phx-click="view" phx-target="<%= @myself %>" value="<%= id %>">
                <span class="icon">
                  <i class="fa fa-eye"></i>
                </span>
              </button>
          </td>
          <td>
              <button class="button is-small is-light mb-0" phx-click="edit" phx-target="<%= @myself %>" value="<%= id %>">
                <span class="icon">
                  <i class="fa fa-edit"></i>
                </span>
              </button>
          </td>
          <td>
            <button class="button  is-small is-light is-danger mb-0" phx-click="delete" phx-target="<%= @myself %>" value="<%= id %>">
                <span class="icon">
                  <i class="fa fa-trash"></i>
                </span>
              </button>
          </td>
          <td></td>
        </tr>
      <% end %>
  </tbody>
</table>
